name: Build and Deploy

on:
  push:
    branches:
    - main

jobs:
  build-and-deploy:
    runs-on: homelab-runner

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Deploy
      run: make deploy
